<p style="color: green"><%= notice %></p>

<h1>Genres</h1>

<div id="genres" class="grid-container">
  <% @genres.each do |genre| %>
    <%= render genre %>
    <p>
      <%= link_to "Show", genre %>
    </p>
  <% end %>
</div>

<%= link_to "New genre", new_genre_path %>
