<%= form_with(model: user_reccord) do |form| %>
  <% if user_reccord.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_reccord.errors.count, "error") %> prohibited this user_reccord from being saved:</h2>

      <ul>
        <% user_reccord.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :library_id, style: "display: block" %>
    <%= form.text_field :library_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
